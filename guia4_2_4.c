/*Declara a, b y c variablen enteras, y, p,q y r variables puntero a entero*/

#include <stdio.h>

int main (){

    int a, b, c, *p, *q, *r;
    //a)
    float m, n, *s, *t;

    //b)
    p=&a;
    q=&b;

    //c)
    

    return 0;
}